{
  "name": "web-audio-graph",
  "version": "0.0.1",
  "description": "A graph implementation for the Web Audio API",
  "main": "lib/Graph.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "jest --config jestConfig.json --watchAll",
    "prepare": "yarn build",
    "doc": "typedoc --theme minimal",
    "prepublishOnly": "yarn test"
  },
  "author": "Valentijn Nieman",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.3",
    "typedoc": "^0.19.2",
    "typescript": "^4.0.5",
    "web-audio-test-api": "^0.5.2"
  }
}
